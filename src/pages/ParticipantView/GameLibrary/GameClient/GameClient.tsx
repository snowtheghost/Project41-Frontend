import Box from '@mui/material/Box';

type Props = {};

const GameClient = (props: Props) => {
  // This is still in development. All the Example mock games should fit inside this container.

  return <Box></Box>;
};

export default GameClient;
