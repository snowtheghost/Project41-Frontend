import CircularProgress from '@mui/material/CircularProgress';

const FullPageSpinner = () => {
  return <CircularProgress sx={{ height: '100%', width: '100%' }} />;
};

export default FullPageSpinner;
